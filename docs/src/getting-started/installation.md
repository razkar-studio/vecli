# Installation

> [!IMPORTANT]
> vecli is a relatively new crate and may still be in development.

vecli is available on [crates.io](https://crates.io/crates/vecli). To add it to your project, run:

```sh
cargo add vecli
```

Or add it manually to your `Cargo.toml`:

```toml
[dependencies]
vecli = "0.3"
```

That's all. vecli has zero dependencies, so it compiles instantly.

---

Next, let's build your [first app](./first-app.md).
